<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/policies_bg"
     >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="15dip"
        android:layout_below="@+id/txtheading" >

        <EditText
            android:id="@+id/dialog_etxt_oldpass"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="10dip"
            android:background="@drawable/editbox_layout"
            android:hint="Current Password*"
            android:inputType="textPassword"
            android:padding="5dip"
            android:singleLine="true"
            android:textStyle="italic" >

            <requestFocus />
        </EditText>

        <EditText
            android:id="@+id/dialog_etxt_re_new_pass"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="10dip"
            android:background="@drawable/editbox_layout"
            android:hint="Re Password*"
            android:inputType="textPassword"
            android:padding="5dip"
            android:singleLine="true"
            android:textStyle="italic" />

        <EditText
            android:id="@+id/dialog_etxt_new_current_pass"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="10dip"
            android:background="@drawable/editbox_layout"
            android:hint="New Password*"
            android:inputType="textPassword"
            android:padding="5dip"
            android:singleLine="true"
            android:textStyle="italic" />

        <LinearLayout
            android:id="@+id/dialog_btn_lay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingBottom="10dip"
            android:paddingTop="15dip" >

            <Button
                android:id="@+id/dialog_save_pass"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_marginRight="5dip"
                android:layout_weight="1"
                android:background="@drawable/btn_yellow"
                android:padding="5dip"
                android:text="Save"
                android:textColor="#ffffff" />

            <Button
                android:id="@+id/dialog_cancel_pass"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_marginLeft="5dip"
                android:layout_weight="1"
                android:background="@drawable/btn_yellow"
                android:padding="5dip"
                android:text="Cancel"
                android:textColor="#ffffff" />
        </LinearLayout>
    </LinearLayout>

    <TextView
        android:id="@+id/txtheading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_marginTop="10dp"
        android:text="Change Password"
        android:textColor="@color/header_bg" />

</RelativeLayout>